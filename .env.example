# afan Talking Head Agent configuration template
#
# 使用方法：把本文件复制一份，重命名为 .env（保留在项目根目录），
# 然后把等号后面替换成你自己的密钥。填好后保存，重新双击"一键启动.bat"。
#
# 更推荐的方式：启动后在网页右上角点「⚙ 设置 → API Key」填写，不用碰文件。
#
# 注意：.env 是你的私人密钥文件，不要发给别人、不要截图、不要上传。

# ── 必填：阿里云百炼（改口型视频必需）────────────────────────
# 申请地址：https://bailian.console.aliyun.com/
# 必须使用「华北2（北京）」地域的 API Key，并在百炼开通 VideoRetalk 服务
# 业务空间 ID：百炼控制台 → 业务空间 → 详情页里的「业务空间ID」
DASHSCOPE_API_KEY=sk-在这里填你的百炼密钥
DASHSCOPE_WORKSPACE_ID=在这里填你的业务空间ID

# ── 必填：MiMo（文案识别、改写、配音）───────────────────────
# 申请地址：https://platform.xiaomimimo.com/
MIMO_API_KEY=在这里填你的MiMo密钥

# ── 选填：DeepSeek（改写的快速模式，不用可不填）──────────────
# 申请地址：https://platform.deepseek.com/
DEEPSEEK_API_KEY=

# ── 选填：Fish Audio（私有音色克隆，不用可不填）──────────────
# 申请地址：https://fish.audio/
FISH_AUDIO_API_KEY=

# ── 选填：FFmpeg 路径（已加入 PATH 或放在 C:\ffmpeg\bin 可不填）──
# 例如 FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe
FFMPEG_PATH=
